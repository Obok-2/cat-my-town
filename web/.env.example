# 이 파일을 .env로 복사해서 쓴다. .env는 Git에서 제외된다.

# 관리자 웹이 호출할 API 주소. 배포는 /api (관리자 도메인 nginx가 백엔드로 넘긴다). 비우면 http://localhost:8080
VITE_API_URL=/api

# 로컬 개발(npm run dev)에서 /api 를 넘길 백엔드 주소 (예: https://백엔드-도메인). 비우면 프록시를 쓰지 않는다
API_PROXY_TARGET=
