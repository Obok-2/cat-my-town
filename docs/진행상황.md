# 진행상황

새 세션을 시작하면 이 문서를 먼저 읽고 지금 상태를 파악한다. **이 문서는 "지금 어떤 상태인지"만 담는다 —**
**누가 언제 무엇을 했다는 이력은 git log가 갖고 있으니 여기 다시 쓰지 않는다.** 작업을 마치면 해당 영역의
섹션을 새 상태에 맞게 고쳐 쓴다(추가하지 말고 덮어쓴다). 「다음에 할 일」도 그때그때 최신 상태로 정리한다.

## 저장소

- GitHub: https://github.com/Obok-2/cat-my-town.git
- 로컬 경로: D:\project\cat_project

## 폴더 구조

```
app/     React Native(Expo, JS) 앱
server/  Spring Boot 3.5 + MyBatis 백엔드
web/     관리자 웹 (React + Vite, JS)
infra/   로컬 개발 인프라 (Docker Compose: PostgreSQL + pgvector, MinIO)
design/  화면 목업 (PDF + HTML 캔버스, 최신은 `우리동네고양이 UIUX 목업/`)
산출물/  설계 산출물 (DB 스키마 초안 schema.sql, 앱 REST API 인터페이스 정의서, ERD는 ERDCloud — 주소는 저장소에 없음)
doc/     기획 문서 — 비어있음
docs/    이 문서
```

## app/ 현재 상태

- 화면: 스플래시·로그인·카메라·매칭결과·이름짓기·도감·고양이상세·레벨·프로필. Google Cloud OAuth의
  ID Token을 서버가 검증한 뒤 자체 JWT(7일, Refresh Token 없음)를 발급하며 앱은 SecureStore에 보관한다.
  Google 네이티브 로그인 때문에 Expo Go가 아닌 Development Build가 필요하다. 촬영 직후 사진은
  `expo-image-manipulator`로 긴 변 최대 1280px·JPEG 품질 0.82로 줄여 `/app/camera/analyze`에 전송하며,
  `NOT_CAT`·`NEW_CAT`·`EXISTING_CAT` 결과 화면까지 서버와 연결됐다. 카메라 하단의 최근 7일 고양이 수도
  `/app/camera/week-count`로 조회한다. 나머지 데이터는 로컬(AsyncStorage) 목데이터다.
- 서버 호출은 `src/api/`의 기능별 모듈에서 axios + async/await로 단순하게 처리한다. 카메라 분석 요청은
  `cameraApi.js`, 신규 고양이 등록과 기존 고양이 목격 기록 요청은 `catApi.js`가 axios 응답을 그대로 반환하고 화면이 필요한 데이터를 꺼내 쓴다.
- 서버가 일치율 40% 이상 후보를 최대 3명 내려주며 앱은 이를 표시한다. 레벨 5단계(`src/data/levels.js`):
  1·5·12·16·30마리.
- 고양이 상세 지도: **카카오 지도 JS SDK로 실제 지도 연동됨**(`KakaoMapView.js`/`.web.js`/`kakaoMapDraw.js`).
  좌표나 키가 없거나 로딩 실패하면 격자 자리표시자로 자동 전환. 키·설정은 `AGENTS.md`「지도」 참고.
  앱(네이티브)은 촬영 탭이 열리면 카메라 → 위치 순서로 권한을 묻고, 셔터를 누를 때 다시 확인한다. 권한이 없거나 위치를 못 잡으면 촬영 흐름을 진행하지 않아 모든 목격 기록에 GPS 좌표가 저장된다(웹은 위치 없이 진행). 카메라·위치 영구 거부 시 설정 열기 안내. 실기기 동작은 아직 확인하지 않았다.
- 실기기·에뮬레이터에서는 아직 아무것도 확인 안 됨(카메라 확대 배율 대략값, 손전등, 지도 WebView 등 전부 웹/헤드리스 브라우저까지만 검증).
- Expo 기본 앱 아이콘을 프로젝트 전용 고양이·위치 핀 마크로 교체했고, 런처·Android adaptive/monochrome·스플래시·favicon 자산을 갖췄다.
  화면 안의 문자 기호도 Ionicons로 통일했으며 라이선스 고지는 `app/THIRD_PARTY_NOTICES.md`에 둔다. 신규 등록 성공 시
  등록 성공 시 Capture·이름 짓기 모달을 `Completion` 일반 화면으로 교체한다. 완료 화면은 레벨업·신규 고양이·기존
  고양이 목격 기록 세 상태에 따라 문구·버튼·전용 고양이 일러스트를 구분하며, 모든 버튼은 도감(`/collection`)으로 이동한다.

## server/ 현재 상태

- Spring Boot 3.5, **JDK 17+ 필요**(로컬 `JAVA_HOME`은 11 — VS Code Java 확장에 딸린 JDK 21로 우회 가능,
  `C:\Users\Admin\.vscode\extensions\redhat.java-*\jre\21.*\bin\java.exe`).
- 구현된 API: `/app/collection/{count,cats}`(도감), `/app/level/count`(레벨), `/app/cat/{detail,markers,sightings}`(고양이 상세),
  `/app/camera/week-count`(최근 7일 목격 횟수·서로 다른 고양이 수),
  `/app/camera/analyze`(DJL 고양이 판별 + Voyage AI/pgvector 기존 개체 후보 최대 3마리),
  `/app/cat/register`(신규 고양이·첫 목격·태그 등록, MinIO 사진 저장, 목격 임베딩·레벨 계산),
  `/app/cat/sighting`(기존 고양이 목격·사진 등록), `/app/photo`(`catId`로 사용자 소유 고양이의 첫 목격 사진 조회). 촬영 분석 응답 상태는
  `NOT_CAT`·`NEW_CAT`·`EXISTING_CAT` 세 가지다. 동일 개체 비교에는 실행 환경의 `VOYAGE_API_KEY`가 필요하다.
  신규 등록과 기존 고양이 목격 등록은 분석 사진을 MinIO 저장용으로 다시 multipart로 받는 MVP 방식이다.
  분석 시 생성한 Voyage 임베딩은 `analysisId`로 서버 메모리에 30분간 보관하고 확정·등록 시 재사용하여 같은 사진으로 Voyage API를 두 번 호출하지 않는다.
  `/app/cat/sighting`은 선택한 고양이가 해당 사용자 소유인지 확인한 뒤 사진을 MinIO에 저장하고
  Voyage AI 임베딩과 목격 기록을 추가하며 레벨은 바꾸지 않는다. 매칭은 각 고양이의 목격 임베딩 중 유사도 상위 2개의 평균을 사용하고 고양이별로 후보 하나만 반환한다.
  코딩 규칙·URL 체계는 `AGENTS.md`「코딩 규칙 (Java)」 참고.
- Spring Security가 자체 HS256 JWT를 검증한다. `/app/auth/google`만 공개이며 나머지 앱 API는 Bearer 인증이 필요하다.
  Google ID Token의 서명·issuer·audience·만료를 검증하고 `sub`를 `users.google_uid`, 이름 대신 `email`을 `users.display_name`에 저장한다(이름은 수집하지 않음). Refresh Token은 사용하지 않으며
  JWT가 만료되면 Google 로그인을 다시 받는다. `infra/.env`는 Git에서 제외된다. `application.properties`는 추적 중이므로
  비밀값이 들어 있는 로컬 변경분은 커밋에서 제외했다. 다른 환경에서는 `GOOGLE_CLIENT_ID`·`JWT_SECRET`을 환경변수로 제공해야 한다.
- 로컬 앱·웹 개발용 CORS는 `WebConfig`에서 전체 경로에 공통 적용한다.
- 앱 카메라 분석과 신규 고양이 등록, 기존 고양이 목격 등록 화면에 연결됨. 도감(`/collection`) 화면은
  `/app/collection/cats`·`/app/collection/count`·`/app/photo` API로 목록, 레벨 진행률, 대표 사진을 조회하며 화면 재진입과
  당겨서 새로고침을 지원한다. 고양이 상세 화면은 `/app/cat/detail`·`/app/cat/markers`·`/app/cat/sightings`를 사용하며
  목격 타임라인을 10건씩 추가 조회한다. 모든 조회 API의 `photoUrl`은 앱에서 바로 표시할 수 있는 `/app/photo?catId=` 또는
  `/app/photo?sightingId=`의 완전한 URL이며, 사진 API는 MinIO 객체를 `inline` 이미지 리소스로 내려준다.
  기존 고양이 후보 선택 뒤에는 목격 기록 작성 화면에서
  메모와 특징 태그(최대 5개)를 입력한 후 저장한다. 촬영 시 `expo-location`으로 위치 권한을 요청하고, 허용된 경우 신규·기존 목격 모두
  위도·경도를 `sightings`에 저장한다. 특징 태그도 고양이 공통 테이블이 아니라 각 `sightings.tags`의 쉼표 구분 값으로 저장한다.
  레벨 화면은 `/app/level/count`를 탭 재진입과 당겨서 새로고침 때 호출하고, 받은 고양이 수를 앱의 등급표로 계산한다.
  등급표는 달성한 등급을 초록색, 현재 등급을 주황색 테두리·그림자, 미달성 등급을 중립색으로 구분한다.
  나머지 앱 데이터는 아직 로컬 목데이터.

## web/ 현재 상태

- 웹 서버 전용 `infra/docker-compose.web.yml`을 작성했다. 볼륨은 `/home/opc/web/dist`, `/home/opc/web/nginx/default.conf`, `/home/opc/web/logs/nginx` 절대 경로이며 HTTP 80을 사용한다. 빌드 파일과 Nginx 설정 파일은 서버에 별도 준비가 필요하다. 실행하지 않았다.
- `/privacy` 개인정보 처리 안내와 푸터 링크를 추가했다. 계정 수집 항목은 Google 이메일·UID로 명시하고, 개인 고양이 기록의 다른 사용자 비공개 및 Voyage AI 사진 전송을 안내한다. 백엔드 계정 수집 변경은 사용자가 별도로 진행한다.
- `/`는 앱 홍보 원페이지: 기능 소개·탭으로 전환하는 예시 화면·사용 방법·FAQ·출시 준비 안내. 기존 앱 일러스트를 재사용하고 모바일 화면에 대응했다.
- 관리자는 `/admin` 로그인, `/admin/dashboard` 등 경로 기반 라우팅으로 분리했다. 배포 서버는 `/admin/*` 직접 접속 시 `index.html`로 연결하는 SPA fallback이 필요하다.
- React + Vite(JS). 로그인·대시보드는 서버 `/admin/auth/login`·`/admin/dashboard`에 axios로 연결됨(`src/api/adminApi.js`, `web/.env`의 `VITE_API_URL=/api`). 배포에서는 mycat-town 서버 nginx의 `location /api/`가 백엔드 서버 도메인의 `/cat/`으로 프록시하고(같은 도메인이라 CORS 불필요, SELinux `httpd_can_network_connect` 켬), 로컬 개발은 `vite.config.js` 프록시가 같은 일을 한다. 관리자 토큰은 저장소에 만료 시각과 함께 두고 401·403이면 로그아웃. 대시보드는 KPI 4개·최근 7일 차트·레벨 분포·최근 로그인(이메일 가림). AI 매칭 도넛은 데이터가 없어 제거. 나머지 메뉴는 "작업 중" 자리표시자 화면.
- 앱과 같은 고양이 마크를 favicon·로그인 화면·관리자 사이드바 브랜드에 적용함.
- 실행: `cd web && npm install && npm run dev`

## infra/ 현재 상태

- 로컬 DB는 호스트 포트 **5455**(기본 PostgreSQL과 충돌 회피), MinIO는 API 9000·콘솔 9001. 실제 DB명·계정명·비밀번호와
  MinIO 관리자 이름·비밀번호는 Git에서 제외된 `infra/.env`에 두고, 키 이름만 `infra/.env.example`로 공유한다.
- `docker-compose.yml`에 Spring Boot 서버 컨테이너 `server`(eclipse-temurin:17-jre)를 추가했다. 외부 포트 **8088**(내부 8080), 볼륨은
  `/home/rocky/my_cat_project/cat_server` → `/app`이며 그 폴더의 `app.jar`를 실행한다. DB·MinIO는 compose 내부 주소(`db:5432`, `minio:9000`)이며,
  접속 정보는 환경변수로 넣지 않고 jar의 `application.properties`에서 읽는다(사용자가 직접 기입). DJL 캐시도 같은 폴더(`.djl.ai`)에 둔다. 실행은 안 해봤다.
- DB·MinIO 데이터도 Docker 이름 볼륨 대신 호스트 폴더 `/home/rocky/my_cat_project/db`·`/home/rocky/my_cat_project/minio`에 저장한다.
- 스키마는 자동 적재 안 됨 — 직접 실행: `docker exec -i cat-my-town-db psql -U catmytown -d catmytown < 산출물/schema.sql`

## 산출물/ 현재 상태

- `schema.sql`: 확정 전 초안. 고치면 ERDCloud의 ERD도 같이 맞출 것(주소는 로컬 메모리에만 있음, 저장소엔 없음).
- `앱_REST_API_인터페이스_정의서.md` v0.3: 구현된 API만 최신 주소·응답 형태 반영, 나머지는 예전 초안 그대로.

## 다음에 할 일

- rocky 서버의 `/home/rocky/my_cat_project/cat_server`에 `app.jar`(접속 정보를 `application.properties`에 넣어 빌드)를 올리고 DB·MinIO용 `infra/.env`를 준비해 `docker compose up -d` 실행(사용자 진행).
- 웹 서버에 `infra/docker-compose.web.yml`을 배치하고 `/home/opc/web`에 빌드 파일·Nginx 설정을 준비. Docker 실행·접속 검증은 별도 사용자 요청 시 진행.
- 홍보·개인정보 안내의 다른 사용자 비공개 정책과 실제 사용자별 접근 제어를 JWT 브랜치 배포 시 함께 확인(사용자 요청 시 검증).
- 개인정보 처리 안내: 사용자 확정 정책은 계정 정보로 Google 이메일·Google UID만 수집, 이름 미수집. 문의용 이메일·보관·파기 기준·외부 AI 처리 세부사항은 추후 보완. `/privacy` 배포 시 SPA fallback 적용.
- 홍보 페이지와 `/admin` 라우팅의 브라우저·모바일 확인(사용자 요청 시). 스토어 출시 후 실제 다운로드 링크 연결.
- `develop` 브랜치(main + `feature/google-login-jwt` 병합)를 확인한 뒤 main 반영 여부 결정(사용자 결정).
- 서버 배포용 `application.properties`는 로컬에서만 값을 하드코딩(DB `db:5432`, MinIO `minio:9000`, Voyage, JWT_SECRET, 매칭 기준값)했다. `GOOGLE_CLIENT_ID`(앱의 웹 클라이언트 ID)도 넣었으며, 이 파일은 비밀값이 있어 커밋하지 않는다.
- 백엔드 서버 앞 nginx(`nginx-web`)는 `/app/` 등을 다른 서비스(8080)가 쓰고 있어 이 서버는 `/cat/` 경로로 `172.17.0.1:8088`에 프록시한다(`/cat` 제거, `X-Forwarded-Prefix /cat`). 서버는 `server.forward-headers-strategy=framework`로 photoUrl을 만들고, 앱 `EXPO_PUBLIC_API_URL`도 관리자 웹과 같은 `https://mycat-town.duckdns.org/api`를 쓴다(웹 서버 nginx `location /api/`에 사진 업로드용 `client_max_body_size 10m` 필요). 사진 `photoUrl`은 마지막 nginx 기준이라 백엔드 도메인으로 만들어진다. 실제 도메인·IP는 공개 저장소에 적지 않고 Git 제외 `.env`에만 둔다.
- 로그인 화면 중앙 고양이 로고 표시 확인 필요(실행 검증 미수행).
- 웹 Google 로그인: DB(`localhost:5455`) 연결 거부 해결. DB 조회 및 서버 상태 API 정상 확인 완료, 사용자 Google 로그인 재시도 필요.
- 실행 중인 로컬 백엔드(`http://localhost:8080`)에 앱 연결 진행 (API 동작 확인은 사용자 요청 시).
- `app/` 실기기·에뮬레이터 확인 (카메라 확대·손전등, 지도 WebView, 스플래시 등)
- Google Cloud Console에 Android OAuth 클라이언트 등록: 패키지 `com.obok2.catmytown`과 Development Build 서명 SHA-1 사용
- `app/`의 나머지 기능을 서버 API에 연결 (`store.js`의 프로필 데이터 관련 호출 교체)
- `server/`: 분석 사진 재전송을 없애는 매칭 세션 도입 검토
- `web/`: 로그인·대시보드를 `/admin/auth/login`·`/admin/dashboard`에 연결(백엔드 완료, 명세는 `산출물/관리자_API_인터페이스_정의서.md`), 배포 DB에 관리자 계정 등록 필요. "작업 중" 메뉴들 구현
- Google 소셜 로그인, Voyage AI/Claude API 연동 키 관리 방식 결정
- 기존 로컬 DB에 `sightings.tags` 컬럼을 추가하고 기존 `cat_tags` 데이터를 옮긴 뒤 테이블 제거

## 작업 기록

- 2026-09-24: 관리자 웹 로그인·대시보드를 서버 API에 연결했다(axios 추가, `src/api/adminApi.js`, `auth.js` 토큰 저장, 대시보드·차트·레벨·최근 로그인 컴포넌트 교체, 사이드바에 로그인한 관리자 표시). 대시보드에서 AI 매칭 통계를 빼고 최근 활동을 최근 로그인 사용자로 바꿨으며 서버가 이메일을 가려서 보낸다. 목데이터(`data/dashboard.js`)와 `MatchDonut`은 삭제. 브라우저 확인·빌드·커밋은 하지 않았다.
- 2026-09-24: 관리자 로그인·대시보드 백엔드 API를 추가했다. `POST /admin/auth/login`(admin_users + BCrypt, 관리자 JWT aud `cat-my-town-admin`·role ADMIN, 12시간), `GET /admin/dashboard`(KPI·최근 7일·레벨 분포·최근 로그인 사용자 10명. AI 매칭 통계는 match_logs를 기록하지 않아 제외). `SecurityConfig`에서 `/admin/**`는 ADMIN, `/app/**`는 USER 토큰만 허용하고 403 응답(`ForbiddenHandler`)을 추가했다. 레벨 기준은 `common.LevelPolicy`로 모았고, CORS에 `https://mycat-town.duckdns.org`를 추가했다. 명세는 `산출물/관리자_API_인터페이스_정의서.md`. 빌드·테스트·커밋은 하지 않았다.
- 2026-09-24: `users.last_login_at`(TIMESTAMPTZ) 컬럼을 추가하고 Google 로그인 upsert에서 가입·재로그인 때마다 `now()`로 갱신하게 했다(`AuthMapper.xml`, `schema.sql`, README). 배포 DB에는 `ALTER TABLE users ADD COLUMN IF NOT EXISTS last_login_at TIMESTAMPTZ;` 적용이 필요하다. ERDCloud ERD도 맞출 것. 빌드·테스트·커밋은 하지 않았다.
- 2026-09-24: 촬영 시 위치를 필수로 바꿨다. 촬영 탭이 열리면 카메라 → 위치 권한을 순서대로 요청하고, 셔터를 누를 때 위치 권한을 다시 확인해 거부(영구 거부면 설정 열기)하거나 GPS로 위치를 못 잡으면 매칭 화면으로 넘어가지 않는다. 카메라 영구 거부 화면에도 설정 열기 버튼을 추가했다. 앱 전체 권한 게이트 시도는 되돌렸다. 실기기 확인·커밋은 하지 않았다.

- 2026-09-24: 프로필 화면의 고양이 수·레벨을 로컬 목데이터(`store.js`) 대신 서버 `/app/level/count`로 조회하도록 바꿨다(레벨 화면과 같은 `computeLevel` 사용). 테스트·커밋은 하지 않았다.

- 2026-09-23: README를 현재 구현 기준으로 갱신했다(소개 사이트 https://mycat-town.duckdns.org 링크, 레벨 5단계, 후보 최대 3마리·40%, ResNet-18 판별, Google 로그인+JWT, 카카오 지도, MinIO, 사용자 입력 태그, 개인정보, 스키마). Claude 설명 생성은 미구현이라 향후 계획으로 옮겼다. 커밋은 하지 않았다.
- 2026-09-23: 프로필 화면의 개발용 버튼(데모 데이터 채우기·도감 비우기)과 핸들러를 주석 처리했다. 로컬 목데이터만 바꾸고 서버 도감에는 반영되지 않기 때문. 테스트·커밋은 하지 않았다.


- 2026-09-23: Google 로그인 시 이름 대신 이메일을 기존 `users.display_name` 컬럼에 저장하도록 `AuthService`만 변경했다(스키마·필드명 유지, 이미 배포된 DB 변경 불필요). 서버 배포용 `application.properties`에 접속 정보를 하드코딩했다(커밋 제외). 빌드·테스트·커밋은 하지 않았다.
- 2026-09-23: 사용자 요청으로 main에서 `develop` 브랜치를 만들어 `feature/google-login-jwt`를 병합했다. 충돌은 이 문서뿐이라 양쪽 내용을 합쳤다. 비밀값을 하드코딩한 `infra/docker-compose.yml` 로컬 변경분은 병합 커밋에서 제외했다. 빌드·테스트·푸시는 하지 않았다.
- 2026-09-23: 사용자 요청으로 `infra/docker-compose.yml`에 JDK 17 서버 컨테이너(`server`, 8088:8080, `/home/rocky/my_cat_project/cat_server` 볼륨)를 추가했다. 실행·테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 사용자 요청으로 `infra/docker-compose.web.yml`과 진행상황 기록을 main 커밋 대상으로 정리했다. 서버 로컬 설정은 제외하며 Docker 실행·테스트는 하지 않았다.
- 2026-09-23: 사용자 요청으로 파일명에서 linux를 빼고 `infra/docker-compose.web.yml`로 변경했다. 실행·커밋·푸시는 하지 않았다.
- 2026-09-23: 사용자 요청으로 웹 서버용 Compose를 `infra/docker-compose.web-linux.yml`로 이동·변경했다. `/home/opc/web` 볼륨 경로는 유지했으며 Docker 실행·테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 사용자 정정에 따라 Nginx Compose 볼륨을 `/home/opc/web` 기준 절대 경로로 바꾸고, 추가로 만들었던 Nginx 설정·배포 문서·환경변수 예제 및 ignore 변경은 되돌렸다. YML만 남겼으며 Docker 실행·빌드·테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 사용자 요청으로 개인정보 안내·기록 비공개 문구·푸터 링크를 main 커밋 대상으로 정리했다. 외부 서비스 문장은 사진의 Voyage AI 전송 안내로 간결하게 수정했다. 서버 로컬 설정은 제외하며 테스트는 실행하지 않았다.
- 2026-09-23: 개인정보 안내의 토큰 저장 구현 설명과 홍보 사이트 이용 문단을 삭제하고, Google 로그인·Voyage AI 사진 전송 사실만 짧게 남겼다. 기존 개인정보 안내 보완 작업은 유지하며 테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 개인정보 안내에서 중복되는 ‘사용자 이름은 수집하지 않습니다’ 문장을 삭제하고 Google 이메일·UID만 수집한다는 문구를 유지했다. 다음 작업은 기존 개인정보 안내 보완 및 백엔드 반영 확인이며 테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 홍보 페이지의 도감 소개·FAQ와 개인정보 안내에 고양이 사진 및 목격 기록(위치 포함)이 다른 사용자에게 공개되지 않는다는 문구를 반영했다. 테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 사용자 확정 정책에 맞춰 웹 개인정보 안내를 Google 이메일·Google UID만 계정 정보로 수집하고 이름은 수집하지 않는 문구로 수정했다. 초안·확정 전·예정 표현은 제거하고 앱 이용 기록은 별도 항목으로 유지했다. 백엔드는 수정하지 않았고 테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 홍보 사이트 푸터에 `/privacy` 개인정보 처리 안내 초안 링크를 추가했다. UID, 이름 미저장 예정 정책, 사진·메모·위치 기록, Voyage AI 전송, 토큰 저장과 미확정 보관·문의 항목을 구분했다. 백엔드는 사용자가 직접 수정한다는 지시에 따라 별도 작업 폴더에 작성했던 이름 저장 관련 변경을 모두 되돌렸다. 테스트·DB 변경·커밋·푸시는 하지 않았다.
- 2026-09-23: 사용자 요청으로 홍보 원페이지·`/admin` 관리자 경로 분리·Obok-2 저작권 표기를 main 커밋 대상으로 정리했다. 서버 로컬 설정은 제외하고 Google 로그인·JWT 브랜치는 병합하지 않는다. 테스트는 실행하지 않았다.
- 2026-09-23: 사용자 지정 활동명 `Obok-2`로 홍보 페이지 푸터에 `© 2026 Obok-2. All rights reserved.` 표기를 추가했다. 테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 사용자 요청으로 `main`으로 전환했다. Google 로그인·JWT 작업은 `feature/google-login-jwt`에 유지하고 병합하지 않았다. `web/`에 앱 홍보 원페이지를 추가하고 관리자를 `/admin` 경로로 분리했다. 서버 로컬 설정 변경은 유지했으며 테스트·커밋·푸시는 하지 않았다.
- 2026-09-23: 사용자 요청으로 Google 로그인·JWT 인증·API 연결과 로그인 화면 수정 사항을 `feature/google-login-jwt` 브랜치에 분리했다. 로컬 비밀값이 포함된 `application.properties` 변경분은 커밋에서 제외한다. main에는 병합하지 않는다.
- 2026-09-23: 로그인 화면 중앙에 남아 있던 목업 자리표시자를 기존 `brand-mark.png` 고양이 로고로 교체했다. 테스트는 실행하지 않았다.
- 2026-09-23: 사용자 요청으로 DB 연결을 검증했다. DB 컨테이너를 실행하고 컨테이너 내부 SQL 조회 성공, 서버 `/actuator/health`의 HTTP 200·UP 응답을 확인했다.
- 2026-09-23: 사용자 로그인 실패 로그를 조사해 500 원인이 PostgreSQL `localhost:5455` 연결 거부임을 확인했다. DB 실행·API 호출은 수행하지 않았다.
- 2026-09-23: 웹 로그인에서 One Tap `prompt()`와 FedCM 비호환 상태 확인 메서드를 제거하고, 로그인 대화상자에 Google 공식 버튼을 표시하도록 변경했다. 취소 처리와 화면 내 오류 표시를 추가했다. 테스트·브라우저 실행 검증은 하지 않았다.
- 2026-09-23: 사용자 요청으로 JDK 21을 사용해 Spring Boot 서버를 백그라운드 실행했다. 시작 로그에서 8080 포트 기동 완료를 확인했다(PID 22056). 테스트·API 호출은 하지 않았다.
