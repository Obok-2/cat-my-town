<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.catmytown.server.app.photo.PhotoDao">

    <select id="selectCatPhotoObjectName" resultType="string">
        SELECT s.photo_url
          FROM cats c
          JOIN sightings s ON s.cat_id = c.id
         WHERE c.id = #{catId}
           AND c.user_id = #{userId}
         ORDER BY s.taken_at, s.id
         LIMIT 1
    </select>

    <select id="selectSightingPhotoObjectName" resultType="string">
        SELECT s.photo_url
          FROM sightings s
          JOIN cats c ON c.id = s.cat_id
         WHERE s.id = #{sightingId}
           AND c.user_id = #{userId}
    </select>

</mapper>
